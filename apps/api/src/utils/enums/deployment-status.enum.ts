export enum DeploymentStatus {
  PENDING = 'pending',
  BUILDING = 'building',
  CLONED = 'cloned',
  BUILT = 'built',
  READY = 'ready',
  STOPPED = 'stopped',
}

//'cloned', 'built', 'failed', 'ready', 'removed', 'building', "stopped"
