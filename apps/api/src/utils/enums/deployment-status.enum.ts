export enum DeploymentStatus {
  PENDING = 'pending',
  BUILDING = 'building',
  CLONED = 'cloned',
  BUILT = 'built',
  READY = 'ready',
  STOPPED = 'stopped',
  DELETED = 'deleted',
}

//'cloned', 'built', 'failed', 'ready', 'removed', 'building', "stopped"
